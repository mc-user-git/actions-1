name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4
 
        - name: Run commands
          run: | 
           echo "My first Github Actions Job"
           ls -ltr
           cat README.md

        # - name: List files
        #   run: ls

        # - name: Read the file
        #   run: cat README.md